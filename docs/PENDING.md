# 대기 중인 후속 작업 (단일 진실)

세션·레포를 넘나드는 다단계 작업의 "다음 공"을 기록한다.
**규칙**: "X 후 Y" 의존 작업이 생기는 순간 여기 한 줄 추가. 완료 시 체크하고 다음 검수 커밋에 함께 반영.

**축 태그** — 제목 앞에 `[검수]`(발행 품질·룰) / `[앱]`(제품 동작) / `[문서]`. 매일 아침 퀴즈
검수(/quiz-audit)는 **`[검수]` 항목과 날짜가 박힌 항목만** 보면 된다. 파일을 쪼개지 않는 이유:
등재 시점에 "어느 파일에 쓸까"를 판단하게 만들면 잘못 넣은 항목이 안 읽히는 쪽으로 사라진다 —
이 파일이 생긴 사고(7/23 3단계 누락)의 재발 경로다. 태그는 틀려도 파일 안에 남는다.

**완료 항목은 한 줄로 적는다.** 근거·수치는 이 파일이 아니라 SSOT(`docs/quality-audit-log.md`,
`docs/rules/`, 커밋)에 있다. 완료 절을 별 파일로 빼지 않는 이유도 위와 같다 — 검수는 결함을 보면
조치를 제안하는 세션이고, "그건 이미 기각됐다"를 막아주는 게 이 목록이다.

## 대기 중

### 날짜가 박힌 것 (먼저 본다)

- [x] ~~**[검수] keyword 카테고리명 가드 효과 추적 (8/6~)**~~ — **8/8 종료.** 8/6·8/7·8/8 세 회차 모두 "용어 == 카테고리명" 0건이고 발행 수 3일 연속 5/5 로 닫는 조건("3일 연속 5/5") 충족. 문자열 동일 비교라 오탐이 원리적으로 없어 남은 축이 발행 수뿐이었다. 근거: `docs/quality-audit-log.md` 8/6~8/8 항목
- [ ] **[검수] 기준 16 효과·부작용 추적 (8/5~, 4일차)**: ① 5/5 발행 유지 여부(개념 포화와 겹치면 4/5 위험) ② 카테고리 이탈 재발 여부. 4/5 발행이 나오면 기준 16 문구 완화. **8/5~8/8 4일 연속 5/5 → 완화 불필요.** ② 는 8/6 411(STOCK 슬롯에 은행 부실채권, 브릿지 없음)로 재발한 뒤 **8/7·8/8 이탈 0건** — 비증시 소재 2회째 유지. 3회째에 문구 재검토를 실험 승격(단발로 건드리지 않는다). 근거: `docs/quality-audit-log.md` "채택 (8/4 오전) — 카테고리 정합"
- [x] ~~**[검수] 8/7 발행분 검수 미실행**~~ — **8/8 백필 완료.** SSH 키 반입 후 8/8 회차에 정답(`is_answer`) 포함 전수 판정으로 함께 검수했다(치명 0 / 경계 2). 재발 방지 축은 아래 `[검수] 검수용 조회 엔드포인트` 항목
- [ ] **[검수] 소재 재등장 차단 — axis 안 기각 후 재설계 대기 (8/8 실험 완료)**: **프롬프트 문구 강화는 이미 배포돼 있고(8/3, 생성·검증 양쪽) axis(뉴스 사건 축) 결정적 차단은 8/8 기각됐다** — 같은 입력에 라벨이 회차마다 달라져(한일↔한미 공동 외환개입) 가드가 비결정적이 된다. 차단 시뮬레이션도 45%↔29% 로 흔들려 발행 수 반토막 위험이 실측됐다. 근거·수치: `docs/quality-audit-log.md` "실험 (8/8 오후)". **재착수 조건: 새 접근 축이 생겼을 때** — 문구 강화 반복·axis 재실험은 하지 말 것. 워크벤치 자산 `POST /api/admin/quizzes/label-axes` 는 남아 있다
- [ ] **[검수] `TERM_GUARD_EXEMPT` 면제 목록 조정 (관측 1회째, 2회째 시 승격)**: 8/8 419 가 8/5 404 와 keyword 용어가 **글자까지 같은 `기준금리`** 인데 본원 용어 면제로 통과했다 — 위 axis 건과 **원인이 다른 별개 건**(각도 변경이 아니라 면제 목록). 수정은 작지만 `기준금리` 를 빼면 INTEREST_RATE 슬롯이 7일에 한 번만 기준금리를 쓸 수 있어 **발행 수 4/5 위험**이 바로 붙는다(개념 포화와 정면 충돌). 변수 분리 원칙상 단독 실험. 근거: `docs/quality-audit-log.md` "실험 (8/8 오후)" 배경 ⓐ
- [x] ~~**[검수] 소재 재등장 차단 — 프롬프트 이력 포맷 개선 실험 (8/8 승격)**~~ — **8/8 기각.** 상세는 위 항목. 원래 등재 문안(참고): 8/3 배포된 이력 주입 포맷이 **각도만 바꾼 같은 소재를 못 잡는다.** 관측 4회 — 8/5 ②(예금금리 축 404·406), 8/6 ②(엔화 축 6일 4회 385·390·395·410), 8/8 ①(기준금리-예금금리 419↔404·406 3일 간격 / [자본 흐름→환율] 423↔400 4일 간격). 공통 원인은 **가드가 keyword 문자열 기준**이라 용어가 달라지면(기준금리/역머니무브, 공동 외환개입/엔 캐리트레이드) 같은 인과 축이 그대로 통과하는 것. 워크벤치(trial_quiz) 실험 후보이며 회귀 하네스로 오탐(정상 문항 반려)을 먼저 잰다. **주의: 개념 포화와 정면 충돌한다** — 차단을 조이면 고정 5카테고리 × 매일 1문항에서 5/5 를 못 채울 수 있어 발행 수를 부작용 축으로 함께 본다(`[검수] 개념 포화 대응` 과 묶어 판단). 근거: `docs/quality-audit-log.md` 8/5·8/6·8/8 항목
- [x] ~~**[앱] 미풀이 북마크 D+2 흐름 확인 (8/7)**~~ — **8/8 확인 완료 (D+3).** ③ 단건 풀이 라우팅 정상(죽은 링크 재발 없음) ② 뱃지 유지 ④ 날짜 고정 열 실계정 확인. ① 목록 순서는 화면으로 판정 불가였는데 원인은 **프론트 목록 DTO 가 `bookmarkedAt` 을 파싱하지 않는 것** — 서버는 `5247cbd` 부터 북마크 목록(`AttemptSummaryResponse`)에 항상 실어 내려주고 있다(미풀이 포함). **백엔드 변경 없음**, 프론트에 파싱+미풀이 행 날짜 표기 이관(8/8 전달). 미풀이 행 제목 잘림 건도 프론트 몫(서버는 치팅 차단 keyword 마스킹 유지)

### 조건 충족 시

- [x] ~~**[검수] 검수 출구 조건**~~ — **8/8 점감 개시.** 주 3회(월·수·금 9:30)로 전환, 사이 날짜는 백필 검수(정상 운영). 조건 충족: 추적 패턴 2개 닫힘 + 기준 16·용어 가드 효과 확인 + 4일 연속 치명 0. **복귀 조건: 치명 결함 재출현 시 매일 체제로.** 스케줄 태스크·레포 스킬 양쪽 반영
- [ ] **[검수] 검수용 조회 엔드포인트 (SSH 키 배포가 실제로 부담이 될 때)**: `~/bin/pinq-quiz-fetch.sh` 가 `~/.pinq-ops.env`(SSH 키·호스트)에 의존해 **새 머신마다 수동 설정이 필요하다**(8/7 Windows 데스크탑에서 확인 — 스킬 문제가 아니었다). 엔드포인트로 옮기면 `curl` 한 줄이 되고 키 배포가 없어진다. **전제: 공개 금지** — 퀴즈 본문은 공개여도 정답(`is_answer`)을 날짜별로 뽑아주면 치트시트라 정답률 밴드(55~75%) 추적이 무의미해진다. 관리자 토큰 가드 필수, 토큰은 레포에 하드코딩 금지. 범위는 스크립트의 3개 서브커맨드(`quizzes`/`logs`/`counts`) 대체. **지금 안 하는 이유**: 머신 하나 세팅은 env 파일 한 개로 끝나므로 비용이 이득을 넘는다
- [ ] **[검수] 해설 서술 품질 — 전담 분리 검증 (명백 사례 재관측 시)**: 8/4 기준 추가 실험 **기각** 후 남은 구조적 후보. 정답 검증(15기준)과 분리된 소형 호출로 해설 문장만 판정. 비용 +1 call/후보. 기각 근거: `docs/quality-audit-log.md` "실험 (8/4 오전)"
- [ ] **[검수] 개념 포화 대응 (관찰 1회째, 2회째 시 실험 승격)**: 8/4 id 400 재생성이 EXCHANGE_RATE 개념 포화로 불가능했던 건 — 고정 5카테고리 × 매일 1문항과 소재 재등장 차단 룰의 충돌. 대안 축 ⓐ 깊이 사다리 ⓑ 동적 카테고리 배분. 상세: `docs/quality-audit-log.md` 8/4 "조치"
- [ ] **[검수] 실험 #9 재실험 (크로스 카테고리 겹침 차단)**: 겹침 2일 연속(7/28 366·367, 7/29 369·371) 로 "후보 실존 시점 재실험" 조건 충족. **아침 발행 직후** 워크벤치 dry-run 으로 기준 14 발동 여부 확인
- [ ] **[앱] 복습 일일 로그가 롤백된 시도까지 센다 (이 경로를 손볼 때, 또는 집계 이상 재관측 시)**: `ReviewDailyLogRecorder` 가 `REQUIRES_NEW` 라 **바깥 채점이 롤백돼도 로그는 커밋된다.** 8/5 졸업 500 사고의 실패 6회가 전부 기록돼 집계가 11/9 로 부풀었다(실제 5/3, 당일 수기 교정). 정상 경로에선 안 생긴다 — 선택지 검증(400)은 기록 이전이라 **기록 이후 터지는 예외**에서만. **순서를 그냥 못 바꾼다**: 졸업 카운터 UPDATE 뒤로 옮기면 `users` 행 락으로 자기 교착(`ReviewService.answerReview` 주석). 후보는 **커밋 후 기록**(`afterCommit`) — 유령은 없어지지만 훅 실패 시 반대로 누락된다. 영향은 `todayReviewed`·`todayCorrect` 와 잔디의 `reviewed` 숫자뿐, **잔디 등급은 무관**
- [ ] **[앱] `weakest` 응답 필드 정리 여부 (이 값을 손볼 일이 생겼을 때)**: 프론트가 절대 기준으로 직접 판정하게 되어 **더 이상 쓰지 않는다**(8/5). 다른 소비자가 없으면 정리 대상이지만 급하지 않다. 규칙: `docs/rules/concept-diagnosis.md` §3.2
- [ ] **[앱] "요즘" 문구 복원 — 기간 필터가 선행 조건**: `countByCategory` 에 기간 조건이 없어 전체 기간 누적이라 "요즘"을 뺐다(8/5). 창(예: 30일)을 도입하면 그 안에서 표본 3개를 못 채우는 사용자가 대부분일 수 있으니 `MIN_ATTEMPTS_FOR_DIAGNOSIS` 도 함께 재검토
- [ ] **[앱] 스토어 이미지 재생성 — 런처 아이콘이 바뀌었다 (다음 배포 때)**: 프론트 `4f4eecc` 에서 아이콘을 **라임 면 + 네이비 새싹 네거티브**로 교체했다(종전 네이비 배경 + 라임 후광). 이것들은 **APK 와 별개로 Play Console 에 수동 업로드**하는 물건이라 배포해도 자동 반영되지 않는다 — 512 아이콘·피처 그래픽·스크린샷 7장이 옛 아이콘을 담고 있다. 뽑는 법·규격·검증법: `../PinQ-frontend/store-assets/README.md`
- [ ] **[문서] 프론트 문서 위치를 도구 기본값에 맞추기 (해당 문서를 건드릴 때 묶어서)**: 옮길 것 ① `PinQ-frontend/docs/{plans,specs}` → `docs/superpowers/{plans,specs}` (superpowers 스킬 지정 위치, 백엔드는 이미 맞음) ② `PinQ-frontend/docs/ui-rules.md` → `docs/rules/ui.md` ③ `plans/review-tree-plan.md` 를 `YYYY-MM-DD-` 컨벤션으로 개명. **방법**: `git mv` + 참조 갱신 + 양 레포 색인 수정을 **같은 커밋에**. 폴더 개명·`archive/` 격리는 기각(비용 대비 이득 없음)

## 완료 (최근)

- [x] 오답노트 목록 메타 정렬 + 글자 크기 (8/7, 프론트 `ff1bb7d`) — 날짜를 오른쪽 고정 열로, 메타 11sp → 12sp. 카테고리를 뺀 행은 글자만 비우고 줄은 남긴다(줄을 없애면 그 행만 키가 작아진다)
- [x] 알림 문구 SSOT 포인터 교체 (8/6, 프론트) — `ui-rules.md` §4 에 소유자와 SSOT 위치만 남기고 문구 원문은 옮기지 않았다. 원문이 두 곳에 생기면 서버가 바뀔 때 낡는다
- [x] 푸시 알림 3건 (8/6, `8de6812`) — ① 본문에서 "방금" 제거 + 문제 수를 그날 실제 발행 수로(상수 4 폐기). **문구 SSOT 는 코드**: `NotificationService.pushBody` ② device-token 등록 멱등화 — check-then-insert 경쟁으로 중복 등록이 500 이던 것 ③ 다중 기기는 이미 지원됨을 확인(변경 없음, 덮어쓰기는 계정 전환 시에만)
- [x] keyword 용어 == 카테고리명 저장 전 폐기 (8/5, `73fc2a5`) — 전수 316건 중 12건(환율 10). 프롬프트 금지 문장이 아니라 **결정적 문자열 비교**로 감. **프롬프트 다이어트는 기각** — 길이 탓이라는 측정이 없고 규칙 제거가 추가보다 위험
- [x] 복습 졸업 채점 500 수정 (8/5, `ae7a48f`) — `clearAutomatically` 가 1차 캐시를 비워 lazy 연관이 끊겼다. 목 테스트로는 못 잡는 자리라 통합 테스트 신설
- [x] 결함 원문 보존 절차 (8/5) — `quiz-audit` 스킬에 편입. **교정 시점이 아니라 발견 시점**에 남긴다(교정 후엔 복원 불가). 레포 스킬 + 스케줄 태스크 사본 양쪽 반영
- [x] 마이페이지 개념 섹션 실서버 확인 (8/5) — 미달 2개 상태에서 빨강 집합 == 배너 집합 일치 확인. 나머지 네 상태는 Showcase 로만 확인됨
- [x] 카테고리 정합 검증(기준 16) 채택 (8/4) — 검증 프롬프트에 배정 카테고리 전달. 회귀 하네스로 적중 2/2·브릿지 예외 2/2
- [x] 인과 룰북 [자본 흐름 → 환율] 축 보강 (8/4) — 사실 명제 누락 보강, 오탐 사전 확인 5/5
- [x] 해설 정합 강화 실험 **기각** (8/4) — 기준 추가 2안 모두 효과 0. 회귀 하네스는 자산으로 남김
- [x] 이력 주입 포맷 + 소재 용어 가드 채택 (8/3) — 7일 내 동일 keyword 용어 재출제 폐기(본원 용어 예외). 상세: `docs/quality-audit-log.md` "채택 (8/3 저녁)"
- [x] 미풀이 북마크 단건 풀이 라우팅 (8/3, 프론트 `ae35597`) — 전일 미풀이 북마크의 죽은 링크 해소. 백엔드 변경 없음
- [x] keyword 용어 2개 병렬 룰 채택 (7/28) — 콜론 2개 이상 리젝. 발행분 276건 전수 대조(적중 4/오탐 0)로 채택
- [x] 잔디 등급 규칙 단순화 (7/28, `f288da5` + 프론트 `6925fd2`) — "하루 4문제 이상 정답 = 라임" 한 축. SSOT: `docs/rules/grass-and-streak.md`
- [x] 오답노트 UI 아코디언 → 목록·상세 분리 (7/28, 프론트 `09a4863`) — API 계약 무변경
- [x] 미풀이 북마크 익일 흐름 확인 (7/26) / 실증 (7/25) — 죽은 링크 발견 → 위 8/3 라우팅으로 해소
- [x] 정원 A안 잔디+나무 통합 (7/22, 프론트 `5fcf04f`) — 백엔드 추가 작업 없음
- [x] 오답노트 목록 경량화 3단계 (7/23, `e97e992`) — ⚠️ 복귀 트리거가 사람 기억뿐이라 누락됐던 건. **이 파일이 생긴 이유**
- [x] 아침 프리워밍 검증 (7/23) / 네이버 API Hub 이관 (7/21, `12298e0`)
