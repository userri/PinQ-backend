# 대기 중인 후속 작업 (단일 진실)

세션·레포를 넘나드는 다단계 작업의 "다음 공"을 기록한다.
**규칙**: "X 후 Y" 의존 작업이 생기는 순간 여기 한 줄 추가. 완료 시 체크하고 다음 검수 커밋에 함께 반영.

**축 태그** — 제목 앞에 `[검수]`(발행 품질·룰) / `[앱]`(제품 동작) / `[문서]` / `[취준]`(이 레포를 소재로 쓰는 면접 준비 — 레포 코드를 바꾸지 않는다). 매일 아침 퀴즈
검수(/quiz-audit)는 **`[검수]` 항목과 날짜가 박힌 항목만** 보면 된다. 파일을 쪼개지 않는 이유:
등재 시점에 "어느 파일에 쓸까"를 판단하게 만들면 잘못 넣은 항목이 안 읽히는 쪽으로 사라진다 —
이 파일이 생긴 사고(7/23 3단계 누락)의 재발 경로다. 태그는 틀려도 파일 안에 남는다.

**완료 항목은 한 줄로 적는다.** 근거·수치는 이 파일이 아니라 SSOT(`docs/quality-audit-log.md`,
`docs/rules/`, 커밋)에 있다. 완료 절을 별 파일로 빼지 않는 이유도 위와 같다 — 검수는 결함을 보면
조치를 제안하는 세션이고, "그건 이미 기각됐다"를 막아주는 게 이 목록이다.

## 대기 중

### 날짜가 박힌 것 (먼저 본다)

- [ ] **[검수] 9/5 발행분 검수 (착수 조건: 06:04 정기 생성 이후)**: **매일 체제다** — 8/14 발행분 치명 2건으로 복귀 조항이 발동했고 사용자가 매일로 결정했다. 주말·평일 구분 없이 매일 돌리며 **빠진 날은 정상 운영이 아니라 결손**이므로 백필로 메운다. 맥의 `pinq-quiz-audit` 크론은 8/17 에 `30 9 * * *` 로 교정됐지만 ⚠️ **맥이 켜져 있어야 돌고, 깨어난 직후 네트워크가 안 붙은 회차는 실패한 채 그날 슬롯을 소진한다**(8/4 실측). 자동 실행은 보조이고 **결손 여부는 사람이 `pinq-quiz-fetch.sh counts` 로 확인한다.** ⚠️ `logs` 는 메모리 링버퍼라 재시작 이전 기록이 없다 — 비어 있는 것과 "SKIP 이 없었던 것"은 다르다. 생성 시도는 `quiz_generation_attempt`, 토큰은 `token_usage` 라 이 결손을 타지 않는다. **8/15 는 생성 로그가 영구 결손이다**(`scrape-stats.jsonl` 에 행 없음 — 0 으로 채우지 않았다): 품질 판정은 뒤늦게라도 되지만 손실 집계는 복구가 안 된다. ⏳ **한시 관측 (8/18 키워드 개편 배포 후 3~5일치가 쌓일 때까지)**: 원시 시도 행을 날짜 루프로 모아 `python3 scripts/keyword-stats.py` 에 넣고 **새 키워드의 SKIP율을 같은 카테고리에서 뺀 키워드와 비교**한다(롤업에는 `searchKeyword` 축이 없어 원시여야 한다). 절대 수치로 줄 세우지 말 것 — 기준선이 카테고리마다 다르다(8/17~18 실측 `코스피` 36% / `물가 상승` 50% / 환율 최저 65%). **8/19 1일차 표본 0** — 앞 키워드(`원달러 환율`·`소비자물가`·`기준금리`·`코스피`·`주택담보대출`)가 전부 발행에 성공해 **새 키워드가 한 건도 안 돌았다.** 즉 이 관측은 달력이 아니라 **마른 날 수**로 세야 한다. **8/20 2일차 — 마른 날이라 처음 돌았고, 하나가 곧바로 무너졌다**: `원화 국제화` 9시도 **SKIP율 100%**(발행 0·룰반려 0·검증실패 0 — 전량 생성 단계 부적합). `디플레이션` 10시도 50%, `물가안정목표` 6시도 SKIP 0% 이나 **6건 전부 룰반려이고 같은 사건(7월 FOMC 의사록)의 다른 매체**라 소재 다양성은 0 이다. `외환보유액`(8/18 오후 추가분) 4시도 중 1발행 — 그 산물이 그날의 치명 480 이다. REAL_ESTATE 신규 3종과 `통화스와프` 는 아직 0회. ⚠️ **표본 1일·9건으로 되돌리지 않는다** — 되돌림 조건은 아래 [앱] 항목이 들고 있다. 관측이 끝나면 **이 문장만 지운다**(스킬·크론·러너 사본은 안 건드린다 — 한시 항목이라 상시 절차로 올리지 않았다). 대상·되돌리기 조건은 아래 [앱] 항목에

- [ ] **[검수] (경계 기록) 옛 `scrape-stats.jsonl` 의 `articles`·재시도 배수는 새 집계와 비교 불가 (8/17 발견·당일 해소, 새 경로만)**: 옛 파서가 `title=` 뒤 꼬리(`stage=`·`reason=`)까지 키로 써서 같은 기사를 여러 건으로 셌다 — **`articles` 는 상한, 재시도 배수는 하한이다**(8/17 EXCHANGE_RATE: 집계 40 vs 고유 29, 1.53× vs 실제 2.10×). 롤업이 `distinctArticles`(URL 기준)를 돌려주도록 고쳐(`6835d14`) **새 경로에서는 해소**됐고 `scripts/scrape-stats.py` 자체는 동결이라 8/12~8/16 행은 그대로다. 남은 착수 조건: **옛 구간의 배수를 근거로 결정을 걸 때 — 그때는 비교 불가로 처리한다.** 근거: `docs/quality-audit-log.md` 8/17 ⓷

- [ ] **[검수] 후보 경쟁 수와 문항 품질의 상관 (8/14 가설 → 8/17 3일차, 방향 일치)**: 시도 건수가 적은 슬롯의 산출물이 나쁘다는 관찰. 8/14 는 시도 2~5건 슬롯(INFLATION·EXCHANGE_RATE)에서 **치명 2건**이 나왔고, 8/16 은 시도 1건뿐인 두 슬롯이 그날 최하위 문항(460 ROE 교과서 정의·462 질문 되풀이)을, 시도 25건을 태운 EXCHANGE_RATE 가 최우수(463)를 냈다. 성립하면 **발행 수 5/5 는 품질 지표가 아니며 시도 수가 선행 지표**라는 뜻이라 검증 강화·포화 대응의 우선순위가 바뀐다. ⚠️ **승격하려면 집계 축이 하나 더 필요하다** — 현행 `scripts/scrape-stats.py` 는 시도만 세고 산출물 등급을 모르므로, 날짜×슬롯에 검수 판정(치명/경계/우수)을 붙여야 상관을 셀 수 있다. 표본 2일이라 아직 관찰. **8/17 3일차도 방향 일치** — 건너뜀 줄이 **0건**인 STOCK(첫 기사가 그대로 통과)이 465(질문 "근거를 설명해야 하는 이유" → 정답 "입증하기 위해서" — 질문 되풀이)로 그날 최하위, 61건을 태운 EXCHANGE_RATE 가 468(명목환율 vs 실질 구매력 층위 구분)로 최우수였다. **다만 8/17 은 최하위가 경계·치명까지 가지 않아 등급 차이로는 안 잡혔다** — 집계 축 없이는 이 상관이 계속 인상 수준에 머문다는 점이 오히려 분명해졌다. 근거: `docs/quality-audit-log.md` 8/14 ③ · 8/16 ④ · 8/17 ⓸

- [ ] **[검수] 검증 2회 판정 실험 (8/14 승격 후보, 표본 1일 — 아직 실험 아님)**: 단발 검증에 **위양성이 상시 존재**함이 처음 실측됐다. 8/14 재투입에서 **453 이 4회 중 1회 true**(449·451·452 는 3/3 true, 450 은 4/4 false) — 같은 입력·같은 모델인데 판정이 갈린다. 즉 검증기가 60% 만 걸러내는 결함은 40% 확률로 발행된다. 안은 **문항당 검증 2회, 둘 다 true 여야 발행**. 비용은 문제가 아니다 — 8/14 계측대로 고정부가 캐시에 올라(verify 12회 중 read 11) read 가 0.1배라 2회가 종전 1회보다 싸다. **8/20 에 근거가 하나 늘었다 — 480(EXCHANGE_RATE)이 복수 정답인 채로 검증을 통과해 발행됐다**(오답 4 "낮은 실질금리 → 해외 투자자 매도 → 환율 상승"이 정답 3과 같은 메커니즘이고, 해설이 오답 4의 문장을 그대로 채용한다). 복수 정답은 검증 기준이 정면으로 금지하는 유형이라 **룰 부재가 아니라 단발 판정의 위양성**이라는 이 항목의 축에 그대로 얹힌다. 다만 아직 재투입을 안 돌려 8/14 같은 회차별 판정 분포가 없다 — **착수 전에 `POST /api/admin/quizzes/480/verify` 재투입부터** (1회 결과로 원인을 귀속하지 않는다). 근거: `docs/quality-audit-log.md` 8/20 치명

  **착수 조건 (셋 다 충족해야 실험 시작)**
  ① **비결정성 재확인 1회 더** — 다른 날 발행분 5문항 × 3회를 같은 방식으로 돌려 true/false 가 갈리는 문항이 또 나오는지. 8/14 하루는 표본이 아니다(단발 사례로 룰을 건드리지 않는다는 원칙).
  ② **harness 차이 통제** — `verifyStoredQuiz` 는 설계상 **이력(recentQuestions)을 안 넣어** 프로덕션 프롬프트와 다르다. 이 상태의 재투입 결과로는 "생성 시점에도 걸렸을 것"을 주장할 수 없다(450 이 4/4 false 인데도 통과한 것이 그 증거다). 실험용으로 이력 주입 옵션을 켤 수 있게 하거나, 워크벤치 dry-run(`test-generate`)에서 2회 판정을 태워 잰다.
  ③ **개념 포화·기준 16 과 변수 분리** — 두 항목 중 하나라도 착수 중이면 발행 수 변화의 원인 귀속이 불가능해진다. 겹치면 순서를 정해 하나씩 돌린다.

  **측정 항목 (실험에서 반드시 같이 잴 것)**
  ⓐ **위음성** — 멀쩡한 문항이 2회째에 떨어지는 비율. 위양성이 줄어드는 만큼 이쪽이 늘며, 이걸 안 재면 "결함이 줄었다"만 보고 발행 수 손실을 놓친다.
  ⓑ **발행 수** — 마른 슬롯(INFLATION·EXCHANGE_RATE)의 4/5 위험. 8/14 에 그 두 슬롯은 시도 2·5건으로 첫 후보가 곧바로 통과했다 — **후보 경쟁이 없는 슬롯에서 2회 판정이 걸리면 대체 후보가 없다.**
  ⓒ 지연 — 정기 06:04 회차의 총 소요. verify 가 2배가 되면 nginx 타임아웃(60s) 여유를 확인해야 한다.

  **기각 조건**: 위양성 감소가 위음성 증가로 상쇄되거나, 발행 5/5 가 4/5 로 내려앉으면 채택하지 않는다 — 룰 강화가 발행 수를 깎는 교환은 이미 여러 번 기각된 유형이다(8/8 axis, 8/4 해설 정합).

  근거: `docs/quality-audit-log.md` "재투입 결과 (8/14 오후)" · 캐싱 계측은 같은 파일 "프롬프트 캐싱 계측 (8/14)"

- [ ] **[검수] 실험 #9 재실험 (크로스 카테고리 겹침 차단)**: 겹침 2일 연속(7/28 366·367, 7/29 369·371) 로 "후보 실존 시점 재실험" 조건 충족. **8/13 에 겹침 1건 재발**(444 근원물가 · 448 임금 vs CPI — 둘 다 7월 CPI 발표라는 같은 사건이 원인). 발행 직후 dry-run 할 후보가 실존한 날이 다시 나온 셈이다. **8/19 에 겹침 재발**(476 STOCK · 478 INFLATION — 둘 다 국제유가 상승 → 국채금리 상승 → 주가 하락으로 **전제 사건과 판단 축이 모두 같다**. 갈리는 것은 경로 설명뿐: 채권 대체수요 vs 할인율). 시도 42건으로 관측 이래 후보 풀이 가장 좁았던 날이라 **풀이 좁으면 서로 다른 슬롯이 같은 기사를 본다**는 형태가 붙는다. **8/28 에 또 겹침**(519 INTEREST_RATE · 522 REAL_ESTATE — 금리 인상 → 변동금리 대출 차주의 즉시 상환 부담 증가로 사건·원인·대비 구도가 같다). ⚠️ **이날은 `CROSS_CATEGORY_USED` 프리필터가 실제로 1건(STOCK) 발동했는데도 이 겹침은 통과했다** — dry-run 의 물음이 "발동하는가"에서 "발동하는데 왜 못 잡는가"로 바뀐다. **8/29·8/30 은 발행분 겹침 0건이고 프리필터 발동도 0건이라 이틀 연속 후보가 실존하지 않았다.** **8/31 은 프리필터가 2건(INFLATION·STOCK) 발동했는데 발행분 겹침은 0건이다** — 8/28(발동 있고 겹침 남음)의 반대 조합이라, "발동과 발행분 겹침이 따로 논다"는 쪽으로 표본이 하나 늘었을 뿐 "왜 못 잡는가"를 가를 표본은 여전히 8/28 1건이다. **9/2 는 발동 1건(STOCK, PREFILTER)인데 발행분 겹침은 0건이라 8/31 과 같은 조합이 한 번 더 나왔다**(545 환율→수입물가 · 546 유가→인플레 우려→나스닥 · 548 근원물가로 하루 세 문항이 물가를 다루나 경로가 각각 환율·유가·지수 정의라 사건이 갈린다 — 두 문항이던 8/31·9/1 보다 한 단계 더 나아간 경계선이다). **9/1 은 발동 0·겹침 0 이라 후보가 실존하지 않았다**(539 INTEREST_RATE·542 REAL_ESTATE 가 둘 다 금리를 다루나 통화정책의 물가 경로 vs 개별 차주의 상품 선택으로 사건·원인이 갈린다 — 8/31 의 환율 두 문항과 같은 경계선이 이틀 연속이다). **9/4 는 발동 1건(EXCHANGE_RATE, PREFILTER)인데 발행분 겹침도 1건이라 8/28 이후 처음으로 "발동했는데 겹침이 남은" 조합이 재현됐다**(554 INTEREST_RATE 빅스텝 인상 기대의 선반영 · 558 INFLATION 기준금리 인상 + 재정 확대 — 출발 원인이 둘 다 기준금리 인상이다. 8/31~9/3 나흘간 "전제는 겹치나 사건·원인은 다르다"로 지나온 경계선을 넘었다고 판단했다). **이로써 "왜 못 잡는가"를 가를 표본이 8/28 에 이어 2건이 됐다** — 두 사례 모두 발동한 슬롯(STOCK · EXCHANGE_RATE)과 겹친 두 문항의 슬롯이 다르다는 공통점이 있다. **아침 발행 직후** 워크벤치 dry-run 으로 기준 14 발동 여부 확인

- [ ] **[검수] 정답 최장 편향 — 문턱 하향 기각, 표본 대기 (8/18 1·2단계 실측)**: **현상은 실재한다** — 발행분 209건 전수에서 정답이 최장인 문항이 **77.5%**(무작위 기대 25%)이고, 비율(정답 글자수 ÷ 오답 평균)이 룰 문턱 1.3 **직하 1.1~1.29 에 70.8% 밀집**한다(1.3 버킷 정확히 0, 1.4 의 3건은 전부 문턱 1.5→1.3 하향 전인 7/11 이전). **그래서 8/12·8/17 의 "5/5" 는 당일 특성이 아니라 이 분포에서 흔한 사건이었고, 승격 사유였던 "재현"은 매일 재현되고 있었다 — 5/5 재관측은 더 이상 신호가 아니다.** ⚠️ **세던 것과 룰이 잡는 것이 다르다**: 룰은 `strictly longest && 비율>=1.3` 의 AND 이고 **동률 최장을 인정하지 않는데**(발행분의 7.2%가 동률) 검수 기록은 동률을 최장 편향에 세어 왔다. **문턱 하향(A안)은 기각한다** — 비용이 1.25 하루 0.91건 / **1.2 하루 1.47건(발행분 30.1%)** 인데, 2단계에서 **피해를 증명하지 못했다.** "길이로 찍어 맞힌다"면 비율이 높을수록 정답률이 높아야 하나 총 시도가 346건뿐이라 문항당 중앙값 1 이고(정답률이 0% 아니면 100% 라 상관 자체가 성립 안 함), 풀링해도 유일최장 68.4%±5.8%p vs 아님 61.5%±9.7%p 로 **구간이 겹치며** 비율 구간별로도 단조가 아니다(문턱에 가장 가까운 1.2–1.3 이 63.7% 로 오히려 낮다). 피해 미확인에 비용은 `TERM_GUARD_EXEMPT`(하루 0.33건, 그 비용 때문에 보류된 건)의 **4.5배**다. **B안(생성 프롬프트 길이 균질화)도 함께 보류** — 효과 신호 없이 토큰만 는다(8/8 axis·8/4 해설 정합과 같은 유형). **재승격 조건(종전 '5/5 재관측' 을 대체): 유일최장·아님 두 갈래가 각각 시도 1,000건.** 그때 95% 구간이 ±3%p 안쪽으로 좁아져 6.9%p 차이를 가릴 수 있다. 지금 속도(하루 8건꼴)로는 몇 달로도 안 되니 **응답이 쌓일 때까지 열어만 둔다.** 방법·수치: `docs/quality-audit-log.md` "실험 (8/18 밤)"

- [ ] **[검수] 기준 16 — 남은 것은 발행 수 감시뿐 (8/12 버그 수정·배포·회귀 완료, 닫는 조건 미충족)**: 8/4 채택 커밋(`4140dd5`)에서 검증 프롬프트의 카테고리·룰북 인자가 뒤바뀌어 **기준 16 이 8일간 판정 근거 없이 돌았다**(`312fe5d` 수정, 회귀 6/6 — 411·438 false, 통과 4건 true, 오탐 0). 그래서 411·438 을 "문구가 못 걸러낸 이탈"로 센 것은 원인 귀속이 틀렸다. **기준 16 이 처음 실제 작동하므로 마른 슬롯(INFLATION·EXCHANGE_RATE)에 4/5 위험이 붙는다 — 닫으려면 정기 회차 단독 5/5 를 봐야 한다**: 8/13~8/16 은 5/5 지만 마지막 칸이 06:10 백필이었고 8/15 는 시도 수 자체가 결손, 8/18 정기 회차는 3/5 다. **8/19 에 처음으로 정기 회차 단독 5/5 가 나왔다**(백필 0회, `cacheWriteTokens` 2,798×1 로 교차 확인). 다만 8/18 최악(3/5) 직후라 진폭으로 보고 **정기 단독 5/5 가 이틀 더 이어지면 닫는다**로 조건을 좁혔는데, **8/20 이 정기 단독 4/5(INFLATION 실패 → 06:10 백필 1칸)라 1일차에서 리셋됐다.** 8/19 의 5/5 는 하루짜리 사건이었고 이틀 세기를 다시 시작한다. **8/22·8/23·8/24 사흘 연속 정기 단독 5/5 로 이 조건이 처음 충족됐다**(8/21 은 EXCHANGE_RATE 백필 1칸으로 다시 리셋됐고, 세기는 8/22 부터다). **8/26 로 닷새째, 8/27 로 엿새째 이어졌으나 8/28 이 정기 단독 4/5(INFLATION 실패 → backfill 3회차·1칸, `cacheWriteTokens` 5,596=2,798×2 로 교차 확인)라 여기서 리셋됐다. 세기를 다시 시작한다.** **8/29 도 정기 단독 4/5 다**(EXCHANGE_RATE 실패 → backfill 1칸, 롤업의 `PUBLISHED`/`BACKFILL` 행으로 교차 확인) — 리셋 상태가 이어졌고, **8/30 이 정기 단독 5/5 라 여기서 1일차부터 다시 셌으나, 8/31 이 정기 단독 3/5(INFLATION·STOCK 실패 → backfill 2칸)라 1일차에서 또 리셋됐다. 백필 2칸은 관측 이래 처음이다. 9/1 은 정기 단독 5/5(backfill 0회, `cacheWriteTokens` 2,798×1 로 교차 확인)라 1일차부터 다시 셌고, 9/2 도 정기 단독 5/5(롤업 56건 전체 REGULAR, `cacheWriteTokens` 2,798×1)라 2일차이고, 9/3 도 정기 단독 5/5(롤업 74건 전체 REGULAR, `cacheWriteTokens` 2,798×1)라 3일차이고, 9/4 도 정기 단독 5/5(롤업 52건 전체 REGULAR, `cacheWriteTokens` 2,798×1)라 4일차다.** ⚠️ **8/27 에 이어 8/28 에도 `CROSS_CATEGORY_USED` 반려가 1건 나왔다**(8/27 INFLATION · 8/28 STOCK, 둘 다 PREFILTER) — 발동 0 이 닫기의 근거였으므로 **그 근거로 닫자는 제안은 성립하지 않는다.** **8/29·8/30 은 다시 0건이었다가 8/31 에 하루 2건(INFLATION·STOCK)으로 최다 발동했고, 9/1 은 0건, 9/2 는 다시 1건(STOCK), 9/3 은 또 0건, 9/4 는 다시 1건(EXCHANGE_RATE)이다 — 0 과 발동이 이틀 간격으로 오가므로 발동 0 을 닫기 근거로 쓰는 길은 더 멀어졌다. 어느 쪽으로도 세지 말고 항목은 열어 둔다.** ⚠️ 더구나 **8/28 은 발동이 있었는데도 발행분 안에 겹침이 남았다**(519 INTEREST_RATE · 522 REAL_ESTATE — 둘 다 "금리 인상 → 변동금리 대출 차주 부담 즉시 증가"). 프리필터가 무엇을 겹침으로 재는지에 대한 물음이라 아래 실험 #9 항목과 함께 본다(1회째, 인과 미확정). 닫을지는 사용자 판단이라 항목은 열어 둔다. ⚠️ 사유 분해로는 기준 16 의 기여를 분리할 수 없다 — `off_category` 가 생성 단계 SKIP 까지 함께 담는다. 근거: `docs/quality-audit-log.md` "발견 (8/12 오후)"

- [ ] **[검수] 검증 프롬프트 캐싱 — 남은 것은 반려 사유 분포 대조뿐 (8/13 배포, 8/14 계측 완료)**: 고정부(룰북 + 검증 기준 1~17 + 응답 형식)를 `OpenAIQuizClient.verifySystemPrompt()` 로 떼어 `system` 블록에 `cache_control: ephemeral` 을 걸었다. **계측 확인**: 8/14 `kind=verify` 12회 중 write 1 / read 11(각 2,798 토큰), 이후 cache_write 량 = 회차 수로 **TTL 5분 예측이 4일 연속 일치**(8/18 ⑩). ⚠️ 판정은 `prompt` 감소가 아니라 **`cache_read>0` 회차 비율**로 한다 — `input_tokens` 는 캐시에 안 걸린 잔여분이라 당연히 준다. ⚠️ 순수 구조 변경이 아니라 **순서 재배치 + 문구 2곳 변경**(문항 위치 명시, 기준 16 의 "위 배정된"→"아래 사용자 메시지의")이라 **반려 사유 분포를 종전과 대조해야 닫힌다.** 불변 테스트 `OpenAIQuizClientPromptTest.verifySystemPrompt_isInvariant` 를 지우지 말 것 — 깨지면 조용히 캐시만 무력화된다

- [ ] **[검수] 기준 16 문구 재검토 — 관측 2회째 (8/12 승격 되돌림, 3회째에 재승격)**: **셈법(확정)**: ① 무브릿지 이탈이 **발행까지 간 건만** 센다(반려·브릿지 있는 통과는 별도 축, 트리거 제외) ② **슬롯 무관 전체 누적** ③ 문구를 바꾼 시점에만 리셋. **현재 2회째: 8/6 411**(STOCK 슬롯 · 은행 부실채권) **→ 8/11 438**(INFLATION 슬롯 · EPU). 3회째 확인 시 실험으로 승격한다. ⚠️ 승격되면 개념 포화와 정면 충돌한다 — 이탈을 조이면 포화된 슬롯이 갈 곳을 잃어 4/5 위험이 붙으므로 그때는 묶어서 판단할 것. **지금은 승격 상태가 아니라 포화 건을 단독 착수할 수 있다.** 발행 수 5/5 감시는 위 `기준 16 재측정` 항목이 들고 있다(여기서 이중으로 세지 않는다). 정의를 다시 세운 경위·축 3개 표: `docs/quality-audit-log.md` "결정 (8/12) — 카테고리 이탈 카운터 정의"

- [ ] **[검수] 난이도 축 — 질문 재진술형 정답 (8/9 승격, 실험 미착수)**: 정답이 질문을 거의 그대로 되풀이하거나 오답 3지가 상식 소거만으로 걸러져 **경제 추론 부담이 사실상 0** 인 문항. 8/5 406·8/6 412·8/8 420·421·422·8/9 426·427 로 **서로 다른 날 5회차**라 당일 기사 풀 편중이 아니라 파이프라인 상시 경향으로 확정. 최극단 표본은 8/9 426(질문 "주식 수익을 주택 구입에 사용하는 현상" → 정답 "주식 수익이 부동산 자금으로 전환되며 자금이 이동하는 메커니즘"). **주의**: 현행 15기준은 정답 유일성만 보고 변별력을 보지 않으므로 기준 추가는 8/4 해설 정합 실험처럼 효과 0 으로 끝날 수 있다 — 워크벤치에서 **먼저 계측**(발행분 표본에 재진술 여부를 라벨링해 비율 확인)한 뒤 룰을 설계한다. 발행 수 직격 위험이 있어 개념 포화 건과 변수 분리. ⚠️ **8/24~9/2 열흘 연속 14회로 관측이 크게 늘었다가 9/3 에 0건으로 끊겼고, 9/4 에 2건(555·557)으로 하루 만에 돌아왔다 — 끊김이 하루짜리였다**(501·506·511·514·523·525·528·530·535·537·538·542·545·547 — 8/29 는 하루 2건, **8/31 은 하루 3건으로 최다이고 9/1 은 1건, 9/2 는 2건, 9/3 은 0건, 9/4 는 2건이다**. 9/3 의 0 은 하루로 끝났고 열이틀 중 열하루가 관측이라 상시 경향이라는 판단이 유지된다). 8/9 승격 당시의 "서로 다른 날 5회차"와 달리 **끊기지 않는 연속**이라 상시 경향이라는 판단이 굳었고, keyword 정답 노출(8/20 이후 열엿새 중 열다섯 번 — 9/3 549 는 keyword 와 정답이 어절 단위로 거의 일치하고, 9/4 557 은 keyword 가 정답이 제시한 용어와 정의를 통째로 담아 선지를 안 읽어도 답이 정해진다)과 두 경로로 답이 새는 문항이 486·506·514·530·535·537·542·545·557 아홉 건이다(8/31 은 하루 2건으로 처음, 9/1·9/2 는 각 1건, 9/3 은 0건, 9/4 는 1건). **착수 순서는 그대로다 — 라벨링 계측이 먼저다.** 근거: `docs/quality-audit-log.md` 8/9 ① · 8/24~9/2 각 항목
- [ ] **[검수] 개념 포화 대응 (8/9 승격, 8/12 설계 착수 — 1단계 측정 대기)**: **설계·계측 SSOT: `docs/superpowers/specs/2026-08-12-concept-saturation-design.md`** (8/9·8/11·8/12 관측 이력은 `docs/quality-audit-log.md` 8/9 ③ · 8/11 ② · 8/12 ①②). **착수 조건: ⓐ 사이클 내 실패 기사 메모 → SKIP 로그에 keyword 추가 → ⓑ 1단계 측정 순.** ⚠️ **포화는 손실의 20% 다** — 1차 원인은 기사 풀 오염(기사-카테고리 부적합 59%)이고 포화는 그 위에 얹힌 2차 원인이다. **지금 숫자로 구조 개조(ⓒ 깊이 사다리·동적 배분)에 착수하지 않는다** — ⓐⓑ 를 걷어낸 뒤 재측정한 손실로 다시 판단한다. ⚠️ 기준 16 이 재승격되면 정면 충돌하므로(이탈을 조이면 포화 슬롯이 갈 곳을 잃는다) 그때는 묶어서 판단할 것 — **8/12 에 승격이 되돌려져 지금은 단독 착수할 수 있다.** ⚠️ 로그가 링버퍼라 재시작하면 계측이 다시 불가능해진다(8/10·8/11 이 그랬다. **8/15 도 검수 결손으로 같은 일이 났다** — 매일 돌지 않으면 이 항목의 계측부터 구멍이 난다). **8/16 계측: 오염이 처음으로 제목만으로 보였다** — EXCHANGE_RATE 가 정기 회차에서 25건을 전량 소진해 실패했는데(사유 `off_category` 13 / `verify_failed` 6 / `duplicate` 4) 후보에 교보문고 베스트셀러·아키하바라 이치방쿠지 체험기·GKL 공모전 시상식이 섞여 있었다. 착수 순서를 바꿀 근거는 아니고 **검색 질의 축이 1순위라는 판단만 강해졌다.** ⚠️ 단 **오염 = 손실로 등치하지 말 것**: 같은 슬롯이 백필에서 그날 최우수 문항(463)을 냈고, 그 소재는 정기 회차에 룰베이스(문장 형태)로 떨어졌다가 재시도가 회수한 것이다 — 실패 건수에는 "소재 없음"과 "형태 불량"이 섞여 있다. 근거: `docs/quality-audit-log.md` 8/16 ①②⑤
- [ ] **[검수] 소재 재등장 차단 — axis 안 기각 후 재설계 대기 (8/8 실험 완료)**: **프롬프트 문구 강화는 이미 배포돼 있고(8/3, 생성·검증 양쪽) axis(뉴스 사건 축) 결정적 차단은 8/8 기각됐다** — 같은 입력에 라벨이 회차마다 달라져(한일↔한미 공동 외환개입) 가드가 비결정적이 된다. 차단 시뮬레이션도 45%↔29% 로 흔들려 발행 수 반토막 위험이 실측됐다. 근거·수치: `docs/quality-audit-log.md` "실험 (8/8 오후)". **재착수 조건: 새 접근 축이 생겼을 때** — 문구 강화 반복·axis 재실험은 하지 말 것. 워크벤치 자산 `POST /api/admin/quizzes/label-axes` 는 남아 있다
- [ ] **[검수] `TERM_GUARD_EXEMPT` 면제 조정 — 비용 실측 완료, 룰 변경 보류 (8/10 승격, 8/18 실측)**: 8/18 에 발행분 209건(7/07~8/18)으로 반사실을 쟀다. **① 비용 상한 하루 0.33건**(43일 14건 = `기준금리` 10·`환율` 4). **② 값싼 다이얼이 없다** — 면제 용어에만 짧은 가드를 거는 안은 1일 3건 / 3일 12건 / 5일 14건인데 **승격 근거 세 사례의 간격이 6·5·6일이라 N≥6 이 필요하고 그 비용은 완전 제거와 같다.** "수정은 작지만"은 코드 크기 얘기였고 효과 축에서는 전부 아니면 전무다. **③ 8/11~8/18 8일간(발행 40건) 걸렸을 건이 어떤 N 에서도 0건** — 지금 배포하면 효과 신호 없이 비용만 관측된다. **재승격 조건(종전 '관측 3회' 를 대체): 8/11 이후 구간에서 연쇄 3회 재관측.** 재승격되면 중간 N 없이 **완전 제거 단독 실험**으로 가고 발행 수를 함께 잰다. 근거: `docs/quality-audit-log.md` "실험 (8/18 오후)"

### 조건 충족 시

- [ ] **[앱] 개편한 검색 키워드의 운영 SKIP율 관측 (착수 조건: 배포 후 3~5일 발행이 쌓였을 때)**: 8/18 저녁에 키워드 63개를 네이버 API 로 직접 재고 4개 카테고리를 갈았다(환율·증시·부동산·물가. **금리는 후보가 전부 검증에 실패해 안 건드렸다**). 판정 근거는 **하루치 스냅샷 1인 육안**이라 운영에서 확인이 필요하다. **할 일:** `scripts/keyword-stats.py` 로 `searchKeyword` 별 SKIP율을 보고 새 키워드가 뺀 키워드보다 나쁘면 되돌린다. **특히 볼 것 —** ⓐ `공매도`(관련 8/10 이지만 과열종목 지정 공시가 4건 중복이라 소재 다양성이 의심스럽다. 채택하되 관측 대상으로 남긴 건이다), ⓑ `상장폐지`·`디플레이션`(그날 사건에 따라 기사 성격이 흔들릴 수 있다), ⓒ 대조군 재측정 — `원화 국제화`·`통화스와프`·`기축통화`는 같은 날 2회 측정에서 결과가 동일했으나 **그건 몇 시간 간격이라 사이클 종속 여부의 증거가 아니다.** 며칠 뒤 같은 방식으로 다시 재 결과가 갈리면 개편안 자체를 다시 본다. ⚠️ **숫자만으로 되돌리지 말 것** — 앞 키워드가 발행에 성공하면 뒤 키워드는 아예 안 돌아, 뒤쪽 키워드는 "마른 날에만 도는" 표본만 쌓여 SKIP율이 불리하게 나온다. 되돌릴 판단이 서면 직접 측정(제목 육안 분류)을 먼저 한다. **근거·방법·분류표:** `docs/quality-audit-log.md` "실험 (8/18 저녁)"


- [ ] **[검수] 검증 반려 사유를 응답에 담기 — 별건 (8/16 분리, 착수 조건 있음)**: `verifyAnswer` 는 **boolean 만** 돌려준다. 그래서 검증기가 **기준 16(카테고리 이탈)으로 깠는지 복수 정답으로 깠는지 알 방법이 없다** — 응답 형식 자체에 사유가 없다. 손실 집계 영속화(아래 항목)로 `VERIFY` 단계와 앞 단계는 분리되지만 **`VERIFY` 안의 세부 사유는 여전히 뭉쳐 있어**, "사유 분해로는 기준 16 의 기여를 분리할 수 없다"는 지적은 절반만 해소된다. ⚠️ **왜 같이 안 했나**: 사유를 받으려면 검증 프롬프트의 **응답 형식(고정부)** 을 바꿔야 하는데 거기에 `cache_control: ephemeral` 캐시가 걸려 있다. 고정부를 건드리면 ① 접두 일치가 깨져 캐시가 무력화될 수 있고(하루 $0.6→$0.15 절감분이 날아간다) ② `OpenAIQuizClientPromptTest.verifySystemPrompt_isInvariant` 가 깨진다 ③ 8/13 선례대로 **문구 변경은 품질 무영향이 아니라 소폭 변경**이라 반려 사유 분포 대조가 따로 필요하다. 즉 변수 분리 원칙상 손실 집계와 동시 착수 불가. **착수 조건: 손실 집계 영속화가 배포돼 며칠 쌓인 뒤, `VERIFY` 단계 손실이 실제로 큰 비중일 때.** 작다면 이 작업은 필요 없다 — 착수 전에 그 비중부터 잴 것. 근거: `docs/superpowers/specs/2026-08-16-loss-stats-persistence-design.md` "한계"

- [ ] **[앱] refresh token rotation 의 GET→DEL 비원자성 — 고칠지 결정 (8/14 발견, 미결정)**: `AuthController.refresh` 가 `resolveUserId`(GET) 로 검증한 뒤 `delete`(DEL) 하고 새 토큰을 발급한다. 두 단계 사이가 원자적이지 않아 **같은 refresh token 이 동시에 두 번 들어오면 둘 다 GET 에 성공해 둘 다 새 토큰을 발급받는다** — rotation 의 "한 번 쓰면 무효" 보장이 동시성 아래서 깨진다. 클라이언트는 마지막 응답만 보관하므로 나머지 하나는 **주인 없는 유효 토큰으로 TTL 14일 동안 남는다.** 재현 경로는 모바일 앱의 요청 재시도·동시 401 처리(여러 API 가 동시에 만료를 만나 각자 /refresh 를 부르는 경우)다. **관측된 사고는 없다** — 코드를 읽고 찾은 것이라 실제 발생 빈도는 미측정. **선택지**: ⓐ `GETDEL`(Redis 6.2+, 현재 7-alpine 이라 가능) 로 조회-삭제를 한 번에 → 진 쪽은 401 을 받는다 ⓑ Lua 스크립트로 같은 효과 + 분기 제어 ⓒ 짧은 grace period(구 토큰 몇 초 유효) — 진 쪽도 살려 사용자 튕김을 막지만 유일성은 여전히 느슨하다 ⓓ 그대로 둔다. ⚠️ **ⓐ 를 고르면 지금 조용히 성공하던 동시 요청이 401 이 된다** — 프론트의 401 처리가 재로그인으로 직행하면 오히려 사용자 경험이 나빠지므로 프론트 재시도 로직을 먼저 확인할 것. **착수 조건: 사용자가 직접 구현한다(8/18 결정) — 세션이 대신 고치지 않는다.** 원인 불명 로그아웃이 관측되면 그때 다시 연다. 면접 답변용 정리는 Notion "Redis — refresh token 저장소로 선택한 이유" 3-4 절. **선택지 ⓔ 추가(8/18 설계)**: `GETDEL` 로 승자를 확정하되 `replay:{옛토큰} → 새 토큰` 을 10초쯤 남겨 **뒤늦은 동시 요청이 같은 새 토큰을 받게** 한다. 고아 토큰이 안 생기면서 **401 도 안 나므로 프론트 재시도 로직을 확인하지 않고도 배포할 수 있다**(ⓐ 의 선행 조건이 사라진다). 대가는 옛 토큰이 탈취됐을 때 그 창 안에서는 현재 토큰을 받아갈 수 있다는 것 — Auth0 등의 leeway 와 같은 절충이다

- [ ] **[취준] MySQL 편 정리 (착수 조건: 면접 준비 시간을 낼 때. 레포 코드는 안 건드린다)**: 노션 `취취취취취준모음 / 소재정리` 아래에 **Redis 편·JWT 편과 같은 4단 구성**으로 MySQL 페이지를 만든다 — ⓪ 실제로 어떻게 쓰나 ① 선택 이유 ② 대안은 없었나 ③ 지금의 단점·극복 방안. 형식 SSOT 는 기존 두 페이지이고 문체·표 형식을 그대로 따른다. ⚠️ **코드를 열어 확인한 사실만 넣는다** — Redis 편에서 `deploy.sh` 를 안 읽고 "배포하면 세션이 날아간다"고 썼다가 사용자 관측에 반박당했다. 관측된 적 없는 결함은 "코드를 읽고 찾은 것"이라고 명시할 것. **확인할 후보**(사실 여부는 착수 시 검증): prod 의 `DDL_AUTO=validate` 와 수기 마이그레이션 절차(`docs/db-access-and-migration.md`, `docs/migration/`), 843MB VM 동거로 인한 자원 제약, 커넥션 풀 설정, `REQUIRES_NEW` 를 쓴 자리들(`ReviewDailyLogRecorder`·`token_usage` 저장)과 그 트랜잭션 경계 판단, 인덱스·조회 패턴, JPA 사용 방식. **대안 절에 쓸 축**: 왜 NoSQL 이 아니라 RDB 인가 / 왜 PostgreSQL 이 아니라 MySQL 인가 / 관리형 RDS 대신 컨테이너 자체 운영을 고른 이유. ⚠️ 두 항목이 이미 SSOT 로 존재하므로 **PENDING·작업기록에 내용을 복제하지 말고 노션에만 쓴다** — 여기엔 "했다/안 했다"만 남는다

- [ ] **[검수] 해설 서술 품질 — 전담 분리 검증 (명백 사례 재관측 시)**: 8/4 기준 추가 실험 **기각** 후 남은 구조적 후보. 정답 검증(15기준)과 분리된 소형 호출로 해설 문장만 판정. 비용 +1 call/후보. 기각 근거: `docs/quality-audit-log.md` "실험 (8/4 오전)"
- [ ] **[앱] 복습 일일 로그가 롤백된 시도까지 센다 (이 경로를 손볼 때, 또는 집계 이상 재관측 시)**: `ReviewDailyLogRecorder` 가 `REQUIRES_NEW` 라 **바깥 채점이 롤백돼도 로그는 커밋된다.** 8/5 졸업 500 사고의 실패 6회가 전부 기록돼 집계가 11/9 로 부풀었다(실제 5/3, 당일 수기 교정). 정상 경로에선 안 생긴다 — 선택지 검증(400)은 기록 이전이라 **기록 이후 터지는 예외**에서만. **순서를 그냥 못 바꾼다**: 졸업 카운터 UPDATE 뒤로 옮기면 `users` 행 락으로 자기 교착(`ReviewService.answerReview` 주석). 후보는 **커밋 후 기록**(`afterCommit`) — 유령은 없어지지만 훅 실패 시 반대로 누락된다. 영향은 `todayReviewed`·`todayCorrect` 와 잔디의 `reviewed` 숫자뿐, **잔디 등급은 무관**
- [ ] **[앱] `weakest` 응답 필드 정리 여부 (이 값을 손볼 일이 생겼을 때)**: 프론트가 절대 기준으로 직접 판정하게 되어 **더 이상 쓰지 않는다**(8/5). 다른 소비자가 없으면 정리 대상이지만 급하지 않다. 규칙: `docs/rules/concept-diagnosis.md` §3.2
- [ ] **[앱] "요즘" 문구 복원 — 기간 필터가 선행 조건**: `countByCategory` 에 기간 조건이 없어 전체 기간 누적이라 "요즘"을 뺐다(8/5). 창(예: 30일)을 도입하면 그 안에서 표본 3개를 못 채우는 사용자가 대부분일 수 있으니 `MIN_ATTEMPTS_FOR_DIAGNOSIS` 도 함께 재검토
- [ ] **[앱] 스토어 이미지 재생성 — 런처 아이콘이 바뀌었다 (다음 배포 때)**: 프론트 `4f4eecc` 에서 아이콘을 **라임 면 + 네이비 새싹 네거티브**로 교체했다(종전 네이비 배경 + 라임 후광). 이것들은 **APK 와 별개로 Play Console 에 수동 업로드**하는 물건이라 배포해도 자동 반영되지 않는다 — 512 아이콘·피처 그래픽·스크린샷 7장이 옛 아이콘을 담고 있다. 뽑는 법·규격·검증법: `../PinQ-frontend/store-assets/README.md`
- [ ] **[문서] 프론트 문서 위치를 도구 기본값에 맞추기 (해당 문서를 건드릴 때 묶어서)**: 옮길 것 ① `PinQ-frontend/docs/{plans,specs}` → `docs/superpowers/{plans,specs}` (superpowers 스킬 지정 위치, 백엔드는 이미 맞음) ② `PinQ-frontend/docs/ui-rules.md` → `docs/rules/ui.md` ③ `plans/review-tree-plan.md` 를 `YYYY-MM-DD-` 컨벤션으로 개명. **방법**: `git mv` + 참조 갱신 + 양 레포 색인 수정을 **같은 커밋에**. 폴더 개명·`archive/` 격리는 기각(비용 대비 이득 없음)

## 완료 (최근)

- [x] 8/20 발행분 검수 (8/20) — **치명 1** / 경계 0. 480 이 복수 정답(오답 4가 정답 3과 같은 메커니즘)인 채 검증을 통과했다 — 치명 0 연속 5일에서 끊김. 정기 단독 4/5 로 기준 16 닫기 조건 리셋, 키워드 개편 관측 2일차에 `원화 국제화` SKIP율 100%, `회차 추정 = cache_write/2798` 이 처음 어긋남(회차 2 / 추정 1 — 회차 간격 2분 59초로 TTL 5분 안)
- [x] 8/19 발행분 검수 (8/19) — 치명 0 / 경계 1. **기준 16 배포 후 첫 정기 회차 단독 5/5**(백필 0회)이고 시도 42건은 관측 이래 최소, 전 슬롯 재시도 배수 1.00×. 크로스 카테고리 소재 겹침이 5일 만에 재발(476·478)
- [x] 8/18 발행분 검수 (8/18) — 치명 0 / 경계 2. 관측 이래 최다 시도 195건인데 **정기 회차 수확은 최저 3/5**(EXCHANGE_RATE·INFLATION 동시에 마른 첫 날, 472·473 은 백필 산물). 토큰은 시도 수와 같은 배율로 늘어 호출당 비용은 불변
- [x] 손실 집계 영속화 — 구현·이관 완료 (8/17) — `quiz_generation_attempt`(시도 1건=1행, `stage`+`reason` 2축, `runWindow` 축, `quiz_id` 존재 필터)로 링버퍼 집계를 대체했다. 검수 6단계도 `attempts` 롤업 + `scripts/attempt-stats.py` → `docs/data/attempt-stats.jsonl` 로 교체. 설계 SSOT: `docs/superpowers/specs/2026-08-16-loss-stats-persistence-design.md`
- [x] `scrape-stats.py` 동결 (8/17) — 링버퍼↔테이블 당일 대조에서 테이블이 상위집합(DB 에만 3건 더). 결함 3종은 용어 가드 반려 누락·카테고리 오귀속·날짜 필터 없음. **삭제가 아니라 동결이다** — 8/12~8/16 은 `docs/data/scrape-stats.jsonl` 이 유일한 기록이고, 사유 분류 기준이 달라 새 파일과 한 계열로 잇지 않는다. 근거: `docs/quality-audit-log.md` 8/17
- [x] 토큰 사용량 영속화 (8/17) — `token_usage` 테이블. 배포 후 첫 회차에서 DB 와 링버퍼가 전 필드 일치. 조회는 `pinq-quiz-fetch.sh tokens`
- [x] `TokenUsageRecorder` 트랜잭션 격리 (8/17, `f90d653`) — `@Transactional(REQUIRES_NEW)` AOP 로는 안 됐다. 바깥 트랜잭션이 rollback-only 로 마킹돼 호출부로 예외가 샜고, `TransactionTemplate` 으로 교체해 막았다
- [x] 8/17 발행분 검수 (8/17) — **치명 0 / 경계 0**(경계 없이 닫힌 날 이틀 연속, 치명 0 재시작 3일차). 백필이 실패한 첫 사례(06:00 정기 4/5 → 06:10 백필 0/1 → 07:10 매시 가드 1/1). 총 시도 102건으로 8/13 ⑩ 의 100+ 조건이 충족돼 재시도 중복이 **회차 사이**에서 난다는 게 확인됐고, 그 과정에서 `scrape-stats.py` 의 `articles` 계측 결함을 발견해 등재. 정답 최장 편향 5/5 재현으로 승격, 토큰 영속화 항목은 첫 계측 일치로 닫음
- [x] 8/15·8/16 발행분 검수 (8/16) — 8/15 치명 0/경계 1(457 오답 4) · 8/16 **치명 0/경계 0**(경계 없이 닫힌 첫 날). 8/15 는 결손 백필이라 생성 로그가 영구 결손이다(집계 행 없음). 8/16 은 EXCHANGE_RATE 25건 전량 실패 → 백필 1/1, 기사 풀 오염이 제목만으로 확인됨
- [x] 매일 체제 복귀 판단 — **복귀 불필요, 점감 체제 유지** (8/12) — 8/10 치명 1(433)로 조건이 형식상 충족됐으나 8/11·8/12 연속 치명 0 으로 표본이 닫혔다. 8/10 치명은 조회 경로 이관으로 검수가 이틀 밀린 구간에서 뒤늦게 발견된 건이고 8/12 는 당일 처리됐다. 스케줄 무변경
- [x] 8/12 발행분 검수 (8/12) — 치명 0 / 경계 1(442 오답 4). 개념 포화 첫 계측·정답 최장 편향 5/5 를 새로 등재
- [x] 윈도우 데스크탑 조회 설정 (8/12) — **홈이 아니라 작업 폴더 안**(`<작업 폴더>/.pinq-ops/.pinq-ops.env`)에 뒀다. 그 폴더는 통째로 지워질 임시 폴더라 비밀을 밖에 두지 않는다. 스크립트가 `$PINQ_OPS_ENV` → 스크립트 실체 기준 `../../.pinq-ops/.pinq-ops.env` → `$HOME` 순으로 찾도록 고쳐(`claude-sync/bin/pinq-quiz-fetch.sh`) 머신 경로를 코드에 박지 않았다 — 맥은 종전대로 `$HOME` 로 떨어진다. SSH 는 쓰지 않았다
- [x] 검수용 조회 엔드포인트 (8/11) — `GET /api/admin/audit/{quizzes,counts,logs}`. SSH 판을 대체한 이유는 키 배포 부담이 아니라 **아웃바운드 22 차단**이었다(사내망에서 키가 있어도 못 붙음). 정답 노출 가드는 기존 `X-Admin-Secret`. `logs` 는 메모리 링버퍼라 재시작 시 비워진다 — 영속이 필요해지면 테이블로 승격
- [x] 검수 주 3회 점감 개시 (8/8) — 월·수·금 9:30, 사이 날짜는 백필 검수(정상 운영). **복귀 조건: 치명 결함 재출현 시 매일 체제로.** 스케줄 태스크·레포 스킬 양쪽 반영
- [x] 소재 재등장 차단 — 이력 포맷 개선 실험 **기각** (8/8) — axis(뉴스 사건 축) 라벨이 회차마다 흔들려 문자열 가드가 비결정적. 재설계 대기 항목이 대기 중에 남아 있다. 근거: `docs/quality-audit-log.md` "실험 (8/8 오후)"
- [x] 미풀이 북마크 D+2 흐름 확인 (8/8, 프론트 `6120174`·`7202457`·`6c20b11`) — 목록 순서 이상은 프론트가 `bookmarkedAt` 을 파싱하지 않은 것(서버는 `5247cbd` 부터 기송신). 백엔드 변경 없음. 제목 잘림은 **현행 질문 한 줄 유지로 닫음** — 카테고리를 올리면 같은 카테고리 미풀이끼리 제목이 같아진다
- [x] keyword 카테고리명 가드 효과 추적 종료 (8/8) — 8/6~8/8 3회차 0건 + 3일 연속 5/5 로 닫는 조건 충족. 문자열 동일 비교라 오탐이 원리적으로 없다
- [x] 8/7 발행분 검수 백필 (8/8) — 정답(`is_answer`) 포함 전수 판정(치명 0 / 경계 2). 재발 방지 축은 `[검수] 검수용 조회 엔드포인트` 항목
- [x] 오답노트 목록 메타 정렬 + 글자 크기 (8/7, 프론트 `ff1bb7d`) — 날짜를 오른쪽 고정 열로, 메타 11sp → 12sp. 카테고리를 뺀 행은 글자만 비우고 줄은 남긴다(줄을 없애면 그 행만 키가 작아진다)
- [x] 알림 문구 SSOT 포인터 교체 (8/6, 프론트) — `ui-rules.md` §4 에 소유자와 SSOT 위치만 남기고 문구 원문은 옮기지 않았다. 원문이 두 곳에 생기면 서버가 바뀔 때 낡는다
- [x] 푸시 알림 3건 (8/6, `8de6812`) — ① 본문에서 "방금" 제거 + 문제 수를 그날 실제 발행 수로(상수 4 폐기). **문구 SSOT 는 코드**: `NotificationService.pushBody` ② device-token 등록 멱등화 — check-then-insert 경쟁으로 중복 등록이 500 이던 것 ③ 다중 기기는 이미 지원됨을 확인(변경 없음, 덮어쓰기는 계정 전환 시에만)
- [x] keyword 용어 == 카테고리명 저장 전 폐기 (8/5, `73fc2a5`) — 전수 316건 중 12건(환율 10). 프롬프트 금지 문장이 아니라 **결정적 문자열 비교**로 감. **프롬프트 다이어트는 기각** — 길이 탓이라는 측정이 없고 규칙 제거가 추가보다 위험
- [x] 복습 졸업 채점 500 수정 (8/5, `ae7a48f`) — `clearAutomatically` 가 1차 캐시를 비워 lazy 연관이 끊겼다. 목 테스트로는 못 잡는 자리라 통합 테스트 신설
- [x] 결함 원문 보존 절차 (8/5) — `quiz-audit` 스킬에 편입. **교정 시점이 아니라 발견 시점**에 남긴다(교정 후엔 복원 불가). 레포 스킬 + 스케줄 태스크 사본 양쪽 반영
- [x] 마이페이지 개념 섹션 실서버 확인 (8/5) — 미달 2개 상태에서 빨강 집합 == 배너 집합 일치 확인. 나머지 네 상태는 Showcase 로만 확인됨
- [x] 카테고리 정합 검증(기준 16) 채택 (8/4) — 검증 프롬프트에 배정 카테고리 전달. 회귀 하네스로 적중 2/2·브릿지 예외 2/2
- [x] 인과 룰북 [자본 흐름 → 환율] 축 보강 (8/4) — 사실 명제 누락 보강, 오탐 사전 확인 5/5
- [x] 해설 정합 강화 실험 **기각** (8/4) — 기준 추가 2안 모두 효과 0. 회귀 하네스는 자산으로 남김
- [x] 이력 주입 포맷 + 소재 용어 가드 채택 (8/3) — 7일 내 동일 keyword 용어 재출제 폐기(본원 용어 예외). 상세: `docs/quality-audit-log.md` "채택 (8/3 저녁)"
- [x] 미풀이 북마크 단건 풀이 라우팅 (8/3, 프론트 `ae35597`) — 전일 미풀이 북마크의 죽은 링크 해소. 백엔드 변경 없음
- [x] keyword 용어 2개 병렬 룰 채택 (7/28) — 콜론 2개 이상 리젝. 발행분 276건 전수 대조(적중 4/오탐 0)로 채택
- [x] 잔디 등급 규칙 단순화 (7/28, `f288da5` + 프론트 `6925fd2`) — "하루 4문제 이상 정답 = 라임" 한 축. SSOT: `docs/rules/grass-and-streak.md`
- [x] 오답노트 UI 아코디언 → 목록·상세 분리 (7/28, 프론트 `09a4863`) — API 계약 무변경
- [x] 미풀이 북마크 익일 흐름 확인 (7/26) / 실증 (7/25) — 죽은 링크 발견 → 위 8/3 라우팅으로 해소
- [x] 정원 A안 잔디+나무 통합 (7/22, 프론트 `5fcf04f`) — 백엔드 추가 작업 없음
- [x] 오답노트 목록 경량화 3단계 (7/23, `e97e992`) — ⚠️ 복귀 트리거가 사람 기억뿐이라 누락됐던 건. **이 파일이 생긴 이유**
- [x] 아침 프리워밍 검증 (7/23) / 네이버 API Hub 이관 (7/21, `12298e0`)
