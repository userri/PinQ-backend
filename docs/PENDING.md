# 대기 중인 후속 작업 (단일 진실)

세션·레포를 넘나드는 다단계 작업의 "다음 공"을 기록한다.
**규칙**: "X 후 Y" 의존 작업이 생기는 순간 여기 한 줄 추가. 완료 시 체크하고 다음 검수 커밋에 함께 반영.
매일 아침 퀴즈 검수(/quiz-audit) 마지막 단계에서 이 파일을 훑는다.

## 대기 중

- [ ] **오답노트 목록 행 글자 크기 (프론트 몫, 착수 조건: 그 화면을 건드릴 때 묶어서)**: 날짜·카테고리가 지금(`labelSmall`) 작다 — 한 단계 키운다(사용자 요청 8/5). 원래 keyword 겹침 건과 한 항목이었으나 그쪽이 백엔드에서 닫히면서 이것만 남았다. 표시 규칙 SSOT: `../PinQ-frontend/docs/ui-rules.md`
- [ ] **keyword 카테고리명 가드 효과 추적 (8/6~, 발행분 며칠 관찰)**: `73fc2a5` 배포분부터 신규 발행에 해당 형태가 0건인지 검수에서 확인. **부작용 축은 발행 수** — 리젝이 재생성을 부르므로 5/5 를 못 채우는 날이 생기는지 본다(기준 16·개념 포화와 겹치면 4/5 위험). 과거 12건은 발행 이력이라 고치지 않는다 — 오답노트에서 옛 문항으로만 보인다
- [ ] **복습 일일 로그가 롤백된 시도까지 센다 (착수 조건: 이 경로를 손볼 일이 생겼을 때, 또는 집계 이상 재관측 시)**: `ReviewDailyLogRecorder` 가 `REQUIRES_NEW` 라 **바깥 채점 트랜잭션이 롤백돼도 로그는 이미 커밋된다.** 2026-08-05 졸업 500 사고에서 실패한 6회가 전부 "물 줬다"로 기록돼 그날 집계가 11/9 로 부풀었다(실제 5/3, 같은 날 수기 교정). 정상 경로에선 안 생긴다 — 선택지 검증(400)은 기록 이전에 끝나므로 **기록 이후 터지는 예외**에서만 발생한다. **순서를 그냥 못 바꾼다**: 기록을 졸업 카운터 UPDATE 뒤로 옮기면 `users` 행 락으로 자기 교착이 난다(`ReviewService.answerReview` 주석 참조). 후보는 **커밋 후 기록**(`TransactionSynchronization.afterCommit`) — 락 충돌도 유령 집계도 없어지지만, 훅이 실패하면 반대로 **누락**된다(과다 대신 과소). 영향 범위는 `todayReviewed`·`todayCorrect` 와 잔디의 `reviewed` 숫자뿐 — **잔디 등급은 무관**하다(등급은 첫 시도 통계인 `countFirstCorrectByDateBetween` 에서 온다)
- [ ] **미풀이 북마크 D+2 흐름 확인 (착수 조건: 2026-08-07)**: 8/5 에 오늘 문제 하나를 **일부러 안 풀고 북마크만** 해뒀다. 익일(D+1)은 8/3 에 이미 확인됐으므로(단건 풀이 라우팅 `ae35597`), 하루를 더 건너뛴 **8/7 에** 북마크 탭을 연다. 볼 것 ① 목록 위치 — 서버 정렬은 북마크 담은 시각 내림차순 한 축이라(`UserBookmarkRepository.findByUserIdOrderByCreatedAtDesc`) 그날 담은 새 북마크들 아래로 밀려 내려가야 정상 ② `solved=false` 뱃지("아직 안 푼 문제") 유지 여부 ③ 클릭 시 **그 문제 단건 풀이**로 가는지 — 오늘 문제 풀기 화면으로 튀면 7/26 에 고친 죽은 링크의 재발 ④ 풀고 나면 정상 이력으로 편입되고 날짜 표기가 담은 날인지 푼 날인지(`bookmarkedAt` 축, `5247cbd`). **서버 쪽에 만료·숨김·상단 고정 로직은 없다** — 이 확인은 전적으로 프론트 렌더/라우팅 검증이다 (착수 조건: 이 값을 손볼 일이 생겼을 때)**: 프론트가 절대 기준으로 직접 판정하게 되어 **더 이상 쓰지 않는다**(8/5). 다른 소비자가 없으면 정리 대상이지만 급하지 않다. 규칙: `docs/rules/concept-diagnosis.md` §3.2
- [ ] **"요즘" 문구 복원 — 기간 필터가 선행 조건**: `countByCategory` 에 기간 조건이 없어 전체 기간 누적이라 "요즘"을 뺐다(8/5). 창(예: 30일)을 도입하면 그 안에서 표본 3개를 못 채우는 사용자가 대부분일 수 있으니 `MIN_ATTEMPTS_FOR_DIAGNOSIS` 도 함께 재검토
- [ ] **프론트 문서 위치를 도구 기본값에 맞추기 (착수 조건: 해당 문서를 건드릴 일이 생겼을 때 묶어서)**: 별도 작업으로 잡지 않는다 — 색인(`docs/README.md`)이 이미 길을 안내하므로 급하지 않다. 옮길 것 ① `PinQ-frontend/docs/{plans,specs}` → `docs/superpowers/{plans,specs}` (superpowers 스킬이 지정하는 저장 위치. `skills/brainstorming/SKILL.md:29`·`skills/writing-plans/SKILL.md:18`, 6.1.1·6.2.0 동일 — 백엔드는 이미 맞고 프론트만 어긋남) ② `PinQ-frontend/docs/ui-rules.md` → `docs/rules/ui.md` ③ 겸사겸사 `plans/review-tree-plan.md` 를 `YYYY-MM-DD-` 접두어 컨벤션에 맞춰 개명. **방법**: `git mv` 로 히스토리 보존 + 참조 갱신 + 양 레포 `docs/README.md` 색인 수정을 **같은 커밋에**. 폴더 개명·`archive/` 격리는 검토 후 기각(비용 대비 이득 없음)
- [ ] **기준 16 효과·부작용 추적 (8/5~)**: ① 5/5 발행 유지 여부(개념 포화와 겹치면 4/5 위험) ② 카테고리 이탈 재발 여부. 4/5 발행이 나오면 기준 16 문구 완화. 근거: `docs/quality-audit-log.md` "채택 (8/4 오전) — 카테고리 정합"
- [ ] **검수 출구 조건 — 조건 충족, 점감 개시 판단만 남음**: 추적 패턴 2개가 모두 닫힘(keyword 계열 7/28 룰 채택 · 카테고리 이탈 8/4 기준 16 채택). 다만 8/4 에 치명 1(400)이 나와 21일 연속 기록이 끊긴 직후이므로, **기준 16·용어 가드의 효과가 며칠 확인된 뒤** 주 2~3회로 점감 (7/23 합의)
- [ ] **해설 서술 품질 — 전담 분리 검증 (명백 사례 재관측 시 착수)**: 8/4 기준 추가 실험 **기각** 후 남은 구조적 후보. 정답 검증(15기준)과 분리된 소형 호출로 해설 문장만 판정. 비용 +1 call/후보라 명백 결함(정답까지 뒤집히는 유형)이 다시 관측되면 착수. 기각 근거·한계: `docs/quality-audit-log.md` "실험 (8/4 오전)"
- [ ] **개념 포화 대응 (관찰 1회째, 2회째 시 실험 승격)**: 8/4 id 400 재생성이 EXCHANGE_RATE 개념 포화로 불가능했던 건 — 고정 5카테고리 × 매일 1문항 구조와 소재 재등장 차단 룰이 충돌. 대안 축 ⓐ 깊이 사다리 ⓑ 동적 카테고리 배분. 재발 시 워크벤치 실험 착수. 상세: `docs/quality-audit-log.md` 8/4 "조치" 절
- [ ] **실험 #9 재실험 (크로스 카테고리 겹침 차단)**: 겹침 2일 연속 발생(7/28 366·367 기대지표 축, 7/29 369·371 주담대 금리 축) — "후보 실존 시점(아침 발행 직후) 재실험" 조건 충족 상태. 아침 발행 직후 워크벤치 dry-run 으로 기준 14 발동 여부 확인

## 완료 (최근)

- [x] keyword 용어가 카테고리명과 겹치는 건 (8/5, `73fc2a5`) — **측정 후 백엔드에서 닫았다.** 발행분 316건 전수: 12건(3.8%)이 용어 == 카테고리 표시명이고 **10건이 환율(15%), 7건이 최근 3주**에 몰려 있었다(내용도 전부 사전적 정의 — `환율: 두 통화 간 교환 비율`). 평균만 보면 "드물다"로 읽히지만 카테고리 편중과 최근 밀도가 그 판단을 뒤집는다. 조치는 **저장 전 결정적 폐기**(문자열 비교) — 프롬프트 금지 문장이 아니라 코드로 간 이유는 ① 프롬프트가 이미 정보량 있는 용어를 요구하는데도 12건이 나왔다 ② 글자까지 같으면 정보량 0이 정의상 참이라 오탐이 원리적으로 0 ③ 비용이 갈리지 않는다(상시 +30토큰 vs 재생성 3.8%, 둘 다 하루 몇 백 토큰). **프롬프트 다이어트는 하지 않기로 함** — 길이 때문에 품질이 떨어졌다는 측정이 없고, 8/4 "기준 추가 효과 0" 은 한계 규칙 포화 신호이지 기존 규칙을 빼도 된다는 신호가 아니다. 규칙마다 사고가 하나씩 붙어 있어 제거가 추가보다 위험하다

- [x] 결함 원문 보존 절차 (8/5) — `quiz-audit` 스킬에 단계로 편입. **교정 시점이 아니라 발견 시점**에 남기도록 정한 게 핵심 — 교정 후엔 DB 에서 복원 불가라 "수기 교정 시" 조건으로는 늦다. 치명 판정이 있는 날만 로그에 `### 원문 보존 — id NNN (교정 전)` 블록으로 question·choices(정답 표시)·explanation·keyword 를 원문 그대로. 레포 스킬과 스케줄 태스크 사본(`~/.claude/scheduled-tasks/pinq-quiz-audit/`) 양쪽에 동일 반영. 배경: 8/4 id 400
- [x] 마이페이지 개념 섹션 실서버 확인 (8/5) — 실기기 실계정에서 **미달 2개** 상태 확인. 배너 `환율 · 부동산 개념이 흔들려요`, 빨강 집합이 배너 집합과 정확히 일치(환율 58%·부동산 58%), 나머지는 라임(물가 82·금리 84·증시 69). 옛 `weakest` 방식이면 둘 중 하나만 빨갛게 됐을 자리라 절대 기준 판정이 실데이터에서도 의도대로 동작함. 다섯 상태 중 나머지 넷은 Showcase 로만 확인된 상태로 남는다(실계정 데이터로는 재현 불가)
- [x] 카테고리 정합 검증(기준 16) 채택 (8/4) — 검증 프롬프트에 배정 카테고리를 전달하도록 구조 보강(종전엔 카테고리가 전달되지 않아 판정 근거 자체가 없었음). 회귀 하네스로 적중 2/2·브릿지 예외 2/2 확인, 대조군 누락 교란 요인 교정
- [x] 인과 룰북 [자본 흐름 → 환율] 축 보강 (8/4) — 실험 아닌 사실 명제 누락 보강. 배포 전 오탐 사전 확인 5/5 통과
- [x] 해설 정합 강화 실험 **기각** (8/4) — 기준 추가 2안 모두 효과 0. 결함군 재평가 결과 325·334 는 정답이 옳고 표현만 하자인 사례라 누수 프레이밍이 과대평가였음. 회귀 하네스는 자산으로 남김
- [x] 이력 주입 포맷 개선 + 소재 용어 가드 채택 (8/3) — ① 같은 카테고리 7일 내 동일 keyword 용어 재출제 결정적 폐기(본원 용어 예외, 코퍼스 오탐 0 검증) ② 프롬프트 이력을 `[용어] 질문` 라인으로 + "같은 소재 각도 변경도 중복" 지시. 총량 규제 3연속(377·382·392) 재발 차단. 효과는 익일부터 검수 "소재 재등장 간격" 항목으로 추적. 상세: `docs/quality-audit-log.md` "채택 (8/3 저녁)"
- [x] 미풀이 북마크 ⓓ 단건 풀이 라우팅 (프론트 `ae35597`, main 병합 + 1.2.0 릴리즈 포함 확인 8/3) — 전일 미풀이 북마크의 죽은 링크 해소. 백엔드 변경 없음. ※ 7/28 "미착수" 표시는 브랜치 병합 전 확인이라 잘못된 정보였음
- [x] keyword 용어 2개 병렬 룰 채택 (7/28) — 콜론 2개 이상 리젝 + 생성 프롬프트 보강. 발행분 276건 전수 대조로 적중 4/오탐 0 확인 후 채택(워크벤치 A/B 대신 코퍼스 검증). 상세: `docs/quality-audit-log.md` "룰 채택 (7/28 저녁)"
- [x] 잔디 등급 규칙 단순화 (7/28, 백엔드 `f288da5` + 프론트 `6925fd2`) — "하루 4문제 이상 정답 = 라임" 한 축으로 축소. 완주·발행수·전부정답 조건 제거로 과거문제 풀이 구멍 원천 해소. 규칙 SSOT: `docs/rules/grass-and-streak.md`
- [x] 오답노트 UI 아코디언 → 목록·상세 분리 (7/28, 프론트 `09a4863` + 백엔드 주석 `425d1bd`) — API 계약 무변경, 용어만 갱신
- [x] 미풀이 북마크 익일 흐름 확인 (7/26) — 북마크 탭에 "풀러가기" 상태로 남음 → 클릭하면 오늘 문제만 뜸 → 전일 미풀이 문제는 풀 수 없음 (죽은 링크). 처리 방침 결정 항목으로 승격
- [x] 미풀이 북마크 실증 (7/25) — 확인된 흐름: 안 푼 문제 북마크 후 뒤로가기 → 북마크 탭에만 "아직 안 푼 문제"로 표시(이력엔 없음) → 클릭 시 문제카드 대신 오늘 문제 풀기 화면으로 이동 → 풀면 푼 문제로 정상 등록. 설계 의도대로 동작
- [x] 정원 A안 잔디+나무 통합 (7/22, 프론트 `5fcf04f` 병합) — 정원=시각 보상, 목록=오답노트 필터칩 이관, 딥링크 포함. 백엔드 추가 작업 없음
- [x] 오답노트 목록 경량화 3단계 (7/23, `e97e992`) — ⚠️ 2단계(프론트) 완료 후 3단계 복귀 트리거가 없어서 동료 질문 전까지 누락됐던 건. 이 파일이 생긴 이유.
- [x] 아침 프리워밍 검증 (7/23, 로그 + 체감 확인)
- [x] 네이버 API Hub 이관 (7/21, `12298e0`)
